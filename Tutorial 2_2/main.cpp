#include <iostream>
#define ARRAY_SIZE(array) (sizeof((array))/sizeof((array[0])))

using namespace std;

int maximum(int arr[], int low, int high)
{
	if (ARRAY_SIZE(arr) == 1)
	{
		return 0;
	}
	int mid = ARRAY_SIZE(arr) / 2;
}

void main()
{

}